libprefix - a simple, effective library for implementing trie-based sets

To install:
./autoconf.sh
./configure
make
sudo make install

To test (after install):
make check

Dependencies:
check ( http://check.sourceforge.net )
automake
libtool
