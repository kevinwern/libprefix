libprefix - a simple, effective library for prefix-suffix based sets, to eventually be expanded to other structures

To install:
./autoconf.sh
./configure
make
sudo make install

To test (after install):
make check

Dependencies:
check ( http://check.sourceforge.net )
automake
libtool
